Doubly linked list:
	Each node in the list has three parts.
		1. previous node's address
		2. node value
		3. next node's address
	we can easily access without using any additional pointers like prev in singly.
	It is more effiecient than singly but more space required than signly
